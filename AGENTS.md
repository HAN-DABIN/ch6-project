# AGENT.md

이 저장소에서 AI Agent가 반드시 따라야 하는 작업 방식을 정의한다.
이 파일은 Agent 작업 규칙의 공통 진실의 원천이다.

---

## 적용 범위

- 이 문서는 `ch6-project` 저장소 전체에 적용한다.
- 하위 디렉터리에 별도 Agent 지침이 생기면 더 가까운 위치의 지침을 우선한다.
- 사용자 요청이 이 문서와 충돌하면 사용자 요청을 우선하되, 충돌 내용을 작업 요약에 기록한다.
- 실제 코드 또는 문서로 확인하지 않은 내용은 가정으로 표시한다.

## 빠른 시작

새 작업을 시작하면 아래 순서로 확인한다.

1. 이 문서의 실행 흐름과 컨텍스트 맵을 확인한다.
2. 요청 도메인에 맞는 정책/API/DB 문서를 읽는다.
3. 실제 코드 구조를 확인한다.
4. `docs/ongoing/`에 작업 계획 또는 진행 상태를 남긴다.
5. 구현 후 Evaluate 결과와 남은 리스크를 기록한다.

## 실행 흐름

모든 작업은 아래 순서로 진행한다.

1. Plan
2. Generate
3. Evaluate

### 1. Plan

- 코드를 작성하기 전에 요구사항을 기능 단위로 해석한다.
- 관련 문서와 기존 코드를 먼저 읽는다.
- 변경 대상, 접근 방향, 통과 기준을 정리한다.
- 구현 전에 사용자 확인이 필요한 경우 질문한다.
- 새 기능이거나 영향 범위가 넓으면 `docs/ongoing/{작업명}.md`에 계획을 작성한다.
- 계획 없이 바로 코드를 생성하지 않는다.

### 2. Generate

- 승인된 계획 범위 안에서만 코드를 작성한다.
- 기존 패키지 구조, 예외 처리, 공통 응답 방식을 따른다.
- 계획에 없는 대규모 리팩터링은 하지 않는다.
- 구현 후 변경 의도와 영향을 기록한다.
- 계획과 다르게 구현해야 할 경우, 먼저 이유를 기록하고 범위를 다시 정리한다.

### 3. Evaluate

- 구현 후 테스트 또는 검증 방법을 확인한다.
- 테스트를 실행하지 못한 경우 그 이유를 기록한다.
- 변경 내용과 남은 리스크를 요약한다.
- 테스트를 실행하지 않았는데 성공했다고 말하지 않는다.
- 실패한 검증도 삭제하지 않고 다음 확인 사항으로 남긴다.

## Agent 작업 체크리스트

작업 시작 전:

- [ ] 요청을 기능 단위로 나눴는가?
- [ ] 관련 정책/API/DB/워크플로우 문서를 읽었는가?
- [ ] 실제 코드 구조와 현재 구현 상태를 확인했는가?
- [ ] 변경 대상과 통과 기준을 정했는가?

작업 중:

- [ ] 계획 범위 밖의 변경을 하지 않았는가?
- [ ] 기존 응답/예외/패키지 구조를 따랐는가?
- [ ] 도메인 정책을 위반하지 않았는가?
- [ ] 판단이 필요한 부분을 문서에 기록했는가?

작업 종료 전:

- [ ] `./gradlew compileJava` 또는 `./gradlew test` 실행 가능 여부를 확인했는가?
- [ ] 실행한 검증과 결과를 사실대로 기록했는가?
- [ ] 미검증 항목과 남은 리스크를 남겼는가?
- [ ] API/DB/정책 문서와 코드가 서로 어긋나지 않는가?

---

## 프로젝트 개요

- 이름: `ch6-project`
- 도메인: 커피숍 주문 시스템
- 목적: 다수 서버 환경에서도 안정적으로 동작하는 커피 주문/결제/인기 메뉴 조회 시스템 구현
- 주요 기능:
    - 회원가입/로그인
    - 커피 메뉴 목록 조회
    - 포인트 충전
    - 커피 주문 및 포인트 결제
    - Kafka 결제 완료 이벤트 발행
    - Redis ZSET 기반 최근 7일 인기 메뉴 Top 3 조회

## 기술 스택

- Java
- Spring Boot
- Spring Security + JWT
- Spring Data JPA
- MySQL
- Kafka
- Redis
- JUnit

---

## 명령어

반드시 Gradle wrapper를 사용한다.

```bash
./gradlew compileJava
./gradlew test
./gradlew bootRun
```

---

## 컨텍스트 맵

아래 상황이 되면 해당 문서를 먼저 읽고 그 규칙을 따른다.
문서가 아직 없다면 필요한 최소 문서부터 생성한다.

| 상황 | 먼저 읽을 문서 |
| --- | --- |
| 작업 계획 수립 | `docs/workflow/plan-guide.md` |
| 코드 구현 | `docs/workflow/generate-guide.md`, `docs/code-convention.md` |
| 테스트/검증 | `docs/workflow/evaluate-guide.md` |
| 개발 로그 작성 | `docs/logs_guide.md` |
| API 명세 확인 | `docs/api/README.md` |
| DB/테이블 명세 확인 | `docs/db/README.md` |
| 도메인 정책 확인 | `docs/policy/README.md` |
| 진행 중인 작업 확인 | `docs/ongoing/` |
| 커밋 메시지 작성 | `docs/commit-convention.md` |

---

## 코드 규칙

- 공통 응답은 `CommonApiResponse`를 사용한다.
- 예외는 `CustomException`과 `ErrorCode`를 사용한다.
- Controller는 요청/응답 처리에 집중한다.
- Service는 비즈니스 로직과 트랜잭션을 담당한다.
- Repository는 데이터 조회/저장 역할만 담당한다.
- Entity와 DTO의 책임을 섞지 않는다.
- 기존 패키지 구조를 우선적으로 따른다.
- 요구사항과 무관한 리팩터링은 하지 않는다.

---

## 도메인 규칙

### 포인트

- 결제는 포인트로만 가능하다.
- 포인트 잔액은 음수가 될 수 없다.
- 포인트 충전/사용 이력은 반드시 기록한다.
- 포인트 차감은 동시성 문제를 고려해야 한다.
- 현재 잔액의 원천 데이터는 `points` 테이블이다.

### 주문/결제

- 주문 금액은 클라이언트가 입력하지 않는다.
- 주문 금액은 서버에 저장된 메뉴 가격을 기준으로 한다.
- 주문, 결제, 포인트 차감은 하나의 트랜잭션으로 처리한다.
- 포인트만 차감되고 주문이 저장되지 않는 상태가 생기면 안 된다.

### 인기 메뉴

- 인기 메뉴는 최근 7일 주문 데이터를 기준으로 한다.
- Redis ZSET을 사용해 메뉴별 주문 횟수를 집계할 수 있다.
- MySQL은 주문/결제 데이터의 진실의 원천이다.
- Redis는 조회 성능 개선을 위한 캐시/랭킹 저장소로 사용한다.
- 인기 메뉴 상세 정보 조회 시 개별 `findById` 반복보다 `findAllById` bulk 조회를 우선 고려한다.

### Kafka

- 주문 결제 완료 후 `PaymentCompletedEvent`를 발행한다.
- Kafka 이벤트는 인기 메뉴 집계 또는 외부 데이터 수집 플랫폼 전송에 사용할 수 있다.
- 이벤트 발행 실패 시 주문/결제와의 일관성 처리 방식을 명확히 기록한다.

---

## 문서화 규칙

- 설계 의도는 `README.md`에 정리한다.
- 작업 계획은 `docs/ongoing/`에 작성한다.
- 작업 과정과 시행착오는 `docs/logs/`에 기록한다.
- 실패한 시도도 삭제하지 않고 기록한다.
- 문서는 실제 코드와 다르게 쓰지 않는다.
- 문서에서 파일명을 언급할 때는 실제 존재하는 경로를 기준으로 쓴다.

---

## 금지 사항

- 기존 사용자 코드를 임의로 삭제하지 않는다.
- 요구사항과 무관한 대규모 리팩터링을 하지 않는다.
- 테스트를 실행하지 않았는데 성공했다고 말하지 않는다.
- 존재하지 않는 클래스, 메서드, 필드를 있다고 가정하지 않는다.
- 공식 문서나 실제 코드로 확인하지 않은 내용을 사실처럼 말하지 않는다.
